'use client';

/**
 * Kho nhạc của quán: cùng màn hình với console chuỗi, nhưng `TracksService`
 * chỉ trả track chung của chuỗi + track quán tự upload (xem `Track.storeId`).
 */
export { default } from '../../dashboard/tracks/page';
