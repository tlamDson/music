'use client';

/**
 * Quán dùng chung màn hình playlist với console chuỗi — backend đã lọc sẵn
 * (STORE_ADMIN chỉ thấy playlist của chuỗi + playlist quán mình). Khi dựng lại
 * giao diện theo ảnh tham chiếu, phần thân sẽ tách thành component dùng chung.
 */
export { default } from '../../dashboard/playlists/page';
